{
  "name": "gym-noob-api",
  "private": true,
  "version": "0.1.0",
  "description": "API-ul de cont și sincronizare pentru Gym Noob (Hono + node:sqlite).",
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "dev": "node src/main.ts",
    "build": "tsc -p .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.8.2",
    "vitest": "^2.1.8"
  }
}
